{
  "name": "@here.build/plexus-do-example-bare",
  "version": "0.0.0",
  "private": true,
  "license": "FSL-1.1-MIT",
  "description": "Fetch-only plexus-do host. ?user= stamps identity; that is not auth.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@here.build/plexus-do": "workspace:^",
    "yjs": "13.6.30"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260910.1",
    "typescript": "6.0.2",
    "wrangler": "^4.106.0"
  }
}
