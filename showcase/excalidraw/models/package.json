{
  "name": "@here.build/plexus-excalidraw-models",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Excalidraw element types as Plexus models. The Scene is the document.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "peerDependencies": {
    "@excalidraw/excalidraw": "^0.18.0",
    "@here.build/plexus": "workspace:*",
    "mobx": "^6.15.0"
  },
  "devDependencies": {
    "@excalidraw/excalidraw": "^0.18.0",
    "@here.build/plexus": "workspace:*",
    "mobx": "6.15.0",
    "typescript": "6.0.2",
    "vitest": "^4.1.0"
  }
}
