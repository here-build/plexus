{
  "name": "@here.build/plexus-xyflow-models",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "xyflow node and edge types as Plexus models. The Flow is the document.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": ["src"],
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "peerDependencies": {
    "@here.build/plexus": "workspace:*",
    "@xyflow/react": "^12.0.0",
    "mobx": "^6.15.0"
  },
  "devDependencies": {
    "@here.build/plexus": "workspace:*",
    "@xyflow/react": "^12.8.2",
    "mobx": "6.15.0",
    "typescript": "6.0.2",
    "vitest": "^4.1.0"
  }
}
